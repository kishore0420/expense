apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: expense
spec:
  selector:
    app: expense-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 32766
  type: NodePort
