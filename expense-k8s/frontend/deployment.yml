apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: expense
  labels:
    app: expense-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: expense-app
  template:
    metadata:
      labels:
        app: expense
    spec:
      containers:
      - name: frontend-container
        image: kishoreking90/frontend:v1
        ports:
        - containerPort: 8080
