apiVersion: v1
kind: service
metadata:
  name: frontend
  namespace: expense
spec:
  selector:
    app: expense-app
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
      NodePort: 32765
  type: NodePort