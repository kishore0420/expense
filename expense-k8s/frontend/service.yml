apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: expense
spec:
  selector:
    app: expense-app
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
      nodePort: 32765
  type: NodePort